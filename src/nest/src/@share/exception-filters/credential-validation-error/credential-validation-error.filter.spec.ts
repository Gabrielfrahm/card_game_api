import { CredentialValidationErrorFilter } from './credential-validation-error.filter';

describe('CredentialValidationErrorFilter', () => {
  it('should be defined', () => {
    expect(new CredentialValidationErrorFilter()).toBeDefined();
  });
});
