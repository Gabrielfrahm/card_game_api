import { NotFoundValidationErrorFilter } from './not-found-validation-error.filter';

describe('NotFoundValidationErrorFilter', () => {
  it('should be defined', () => {
    expect(new NotFoundValidationErrorFilter()).toBeDefined();
  });
});
