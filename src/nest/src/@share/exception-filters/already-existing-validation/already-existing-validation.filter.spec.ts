import { AlreadyExistingValidationFilter } from './already-existing-validation.filter';

describe('AlreadyExistingValidationFilter', () => {
  it('should be defined', () => {
    expect(new AlreadyExistingValidationFilter()).toBeDefined();
  });
});
